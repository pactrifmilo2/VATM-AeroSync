<Page
    x:Class="AeroSync.UI.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">
    <ScrollViewer>
        <StackPanel Padding="24" Spacing="14">
            <Grid>
                <StackPanel>
                    <TextBlock Text="HTSLB OPERATOR › GIÁM SÁT ĐỒNG BỘ" FontSize="26" FontWeight="Bold" Foreground="#123A70" />
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#64748B" />
                </StackPanel>
                <Button HorizontalAlignment="Right" Content="Refresh" Command="{Binding RefreshCommand}" />
            </Grid>

            <Border Background="#EFF6FF" BorderBrush="#3B82F6" BorderThickness="1" CornerRadius="8" Padding="14">
                <StackPanel Spacing="5">
                    <TextBlock Text="THÔNG BÁO THƯ MỤC INCOMING" FontWeight="Bold" Foreground="#1D4ED8" />
                    <TextBlock Text="{Binding IncomingNotice}" TextWrapping="Wrap" Foreground="#1E3A5F" />
                </StackPanel>
            </Border>

            <Grid ColumnSpacing="16" RowSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" BorderBrush="#B7E4C7" BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="SUCCESS" Foreground="#16A34A" FontWeight="Bold" />
                        <TextBlock Text="{Binding SuccessCount}" Foreground="#16A34A" FontSize="30" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="ERROR" Foreground="#DC2626" FontWeight="Bold" />
                        <TextBlock Text="{Binding ErrorCount}" Foreground="#DC2626" FontSize="30" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" BorderBrush="#FED7AA" BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="WARN" Foreground="#EA580C" FontWeight="Bold" />
                        <TextBlock Text="{Binding WarnCount}" Foreground="#EA580C" FontSize="30" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="3" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="SKIPPED" Foreground="#2563EB" FontWeight="Bold" />
                        <TextBlock Text="{Binding SkippedCount}" Foreground="#2563EB" FontSize="30" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="4" BorderBrush="#DDD6FE" BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="PROCESSING" Foreground="#7C3AED" FontWeight="Bold" />
                        <TextBlock Text="{Binding ProcessingCount}" Foreground="#7C3AED" FontSize="30" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="5" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="TOTAL" Foreground="#334155" FontWeight="Bold" />
                        <TextBlock Text="{Binding TotalJobs}" Foreground="#334155" FontSize="30" FontWeight="Bold" />
                    </StackPanel>
                </Border>
            </Grid>

            <Border BorderBrush="#D6E3F3" BorderThickness="1" CornerRadius="8" Padding="14">
                <StackPanel Spacing="12">
                    <TextBlock Text="TÌM KIẾM &amp; BỘ LỌC" FontSize="18" FontWeight="Bold" Foreground="#0B4EA2" />
                    <Grid ColumnSpacing="18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>
                        <DatePicker Grid.Column="0" Header="Thời gian" />
                        <ComboBox Grid.Column="1" Header="Nguồn" SelectedIndex="0">
                            <ComboBoxItem Content="Tất cả" />
                            <ComboBoxItem Content="Email" />
                            <ComboBoxItem Content="Thư mục" />
                        </ComboBox>
                        <ComboBox Grid.Column="2" Header="Trạng thái" SelectedIndex="0">
                            <ComboBoxItem Content="Tất cả" />
                            <ComboBoxItem Content="SUCCESS" />
                            <ComboBoxItem Content="FAILED" />
                            <ComboBoxItem Content="QUARANTINED" />
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

            <Border BorderBrush="#D6E3F3" BorderThickness="1" CornerRadius="8" Padding="14">
                <StackPanel Spacing="10">
                    <TextBlock Text="DANH SÁCH ĐỒNG BỘ DỮ LIỆU" FontSize="18" FontWeight="Bold" Foreground="#0B4EA2" />
                    <controls:DataGrid
                        x:Name="JobsGrid"
                        AutoGenerateColumns="False"
                        GridLinesVisibility="All"
                        HeadersVisibility="Column"
                        IsReadOnly="True"
                        ItemsSource="{Binding Jobs}"
                        SelectionChanged="JobsGrid_SelectionChanged"
                        RightTapped="JobsGrid_RightTapped">
                        <controls:DataGrid.Columns>
                            <controls:DataGridTextColumn Header="NGƯỜI GỬI / FILE" Binding="{Binding DisplayLabel}" Width="*" />
                            <controls:DataGridTextColumn Header="TRẠNG THÁI" Binding="{Binding Status}" Width="180" />
                            <controls:DataGridTextColumn Header="THỜI GIAN" Binding="{Binding TimeLabel}" Width="180" />
                        </controls:DataGrid.Columns>
                    </controls:DataGrid>
                </StackPanel>
            </Border>

            <Border BorderBrush="#D6E3F3" BorderThickness="1" CornerRadius="8" Padding="14">
                <StackPanel Spacing="10">
                    <TextBlock Text="LOG SYSTEM REAL-TIME" FontSize="18" FontWeight="Bold" Foreground="#0B4EA2" />
                    <ListView ItemsSource="{Binding Logs}" MaxHeight="160">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayLine}" FontFamily="Consolas" />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
