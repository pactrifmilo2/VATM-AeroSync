server:
  port: 8080

spring:
  config:
    import: optional:file:${AEROSYNC_CONFIG_DIR:${user.dir}}/.env[.properties]
  application:
    name: aerosync-api

  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/XEPDB1
    username: vatm_user
    password: vatm_password
    driver-class-name: oracle.jdbc.OracleDriver

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  amqp:
    deserialization:
      trust:
        all: true

  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    properties:
      hibernate:
        boot:
          allow_jdbc_metadata_access: false
        default_batch_fetch_size: 100
        hbm2ddl:
          jdbc_metadata_extraction_strategy: individually
      jakarta:
        persistence:
          database-product-name: Oracle
          database-major-version: 21
          database-minor-version: 3

app:
  api:
    defaults:
      scheduler-fixed-delay-ms: 300000
      max-files-per-cycle: 100
      blacklist-senders:
        - ops@vatm.local
  test-replay:
    enabled: ${APP_TEST_REPLAY_ENABLED:false}
    atfm-write-enabled: ${APP_ATFM_WRITE_ENABLED:false}
    atfm-url: ${APP_ATFM_DATASOURCE_URL:}
    atfm-username: ${APP_ATFM_DATASOURCE_USERNAME:}
    atfm-password: ${APP_ATFM_DATASOURCE_PASSWORD:}
  rabbit:
    sync-result-exchange: sync.result
    dashboard-alerts-queue: dashboard.alerts.queue
    file-ingested-exchange: file.ingested
    file-processing-routing-key: file.processing
